#include <iostream>

// #include <glib.h>
// #include <NetworkManager.h>

int main(int argc, char *argv[]) {
    // NMClient *client = nullptr;

    // client = nm_client_new(nullptr, nullptr);

    // if (client) {
    //     std::cout << "Client version" << nm_client_get_version(client) << std::endl;
    // }

    // const GPtrArray *connections = nm_client_get_connections(client);

    // NMRemoteConnection* connection = ((NMRemoteConnection *)connections->pdata[0]);

    // std::cout << "Is visible " << nm_remote_connection_get_visible(connection) << std::endl;
    std::cout << "Hello World" << std::endl;
}
